pipeline {
    agent {
        label 'electronix'
    }

    stages {

        stage('Tools configure') {
            steps {
                echo 'Tools Installations pipeline'
            }
        }

        stage('List update') {
            steps {
                sh '''
                    sudo apt-get update -y
                '''
            }
        }

    }
}